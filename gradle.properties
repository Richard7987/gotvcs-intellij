# Optional: set `ideLocalPath` in ~/.gradle/gradle.properties (machine-local,
# not versioned) to compile against a local IntelliJ installation instead of
# downloading one. See README.md "Building" section.

kotlin.stdlib.default.dependency=false
